set(THREAD_SAFE_LOGGER_HEADERS
    "${CMAKE_CURRENT_SOURCE_DIR}/ThreadSafeLogger.h")

add_library(${THREAD_SAFE_LIBRARY_NAME} INTERFACE)
target_include_directories(${THREAD_SAFE_LIBRARY_NAME} INTERFACE ${CMAKE_CURRENT_SOURCE_DIR})